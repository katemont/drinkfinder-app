%h1 Bars
%table
  %thead
    %tr
      %th Name
      %th Address
      %th 
      %th 
      %th
  %tbody
    - @bars.each do |bar|
      %tr
        %td= bar.name
        %td= bar.location
        / %td= bar.longitude
        / %td= bar.latitude

        %td= link_to 'Show', bar
        %td= link_to 'Edit', edit_bar_path(bar)
        %td= link_to 'Destroy', bar, method: :delete, data: { confirm: 'Are you sure?' }
